package com.deccan.petroconnect.services;

import org.springframework.stereotype.Service;

/**
 * JWT Token Provider - DEPRECATED
 * Using session-based authentication instead.
 * Kept for reference but not used in current implementation.
 */
@Service
public class JwtTokenProvider {
    // Deprecated - sessions handle authentication now
}